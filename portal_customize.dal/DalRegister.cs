using System;
using Microsoft.Extensions.DependencyInjection;

namespace portal_customize.dal
{
    public class DalRegister
    {
        public void DIRegister_DAL(IServiceCollection services)
        {
            //services.AddTransient(typeof(IMessage), typeof(Message));
            //services.AddTransient(typeof(ITheme), typeof(Theme));
            //services.AddTransient(typeof(IBadge), typeof(Badge));
            //services.AddTransient(typeof(IGreeting), typeof(Greeting));
        }
    }
}
